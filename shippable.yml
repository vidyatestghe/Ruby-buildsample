language: ruby

rvm:
  - jruby-head

env:
  - CI_REPORTS=shippable/testresults COVERAGE_REPORTS=shippable/codecoverage

build_image: shippable/minv2:beta

before_install:
  #- source /home/shippable/.rvm/scripts/rvm
  - source ~/.rvm/scripts/rvm && rvm use jruby

branches:
  except:
     - develop
before_archive:
  - echo "hi"

  
after_archive:
  - echo "hello"
